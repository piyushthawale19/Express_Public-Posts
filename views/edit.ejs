<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <nav>
        <a  class="px-3 py-2 text-purple-400 hover:text-black hover:bg-purple-600 rounded hover:underline" href="/profile">Profile</a>
      </nav> 
      <div class="w-full flex justify-end">
        <a
          class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block hover:bg-red-700"
          href="/logout"
          >Logout</a
        >
      </div>
      <h5 class=" mb-5 underline">Edit Your Post.</h5>
      <form action="/update/<%= post._id %>" method="post">
        <textarea
          placeholder="What's on your mind ?"
          class="block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"
          name="content"
        ><%= post.content %></textarea>
        <input
          class="px-3 py-2 w-40 bg-yellow-400 text-black hover:bg-yellow-500 block cursor-pointer rounded-md mt-3 text-sm"
          type="submit"
          value="Update Post"
        />
      </form>
    </div>
  </body>
</html>
