<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <nav class="w-full flex justify-between items-center mb-20">
  <a class="px-3 py-2 text-purple-400 hover:text-black hover:bg-purple-600 rounded hover:underline" href="/profile/upload">
    EditProfile
  </a>
  <a class="bg-red-500 rounded-md px-3 py-2  text-sm inline-block hover:bg-red-700" href="/logout">
    Logout
  </a>
</nav>
       
      <div class="flex items-start gap-3">
        <div class="w-10 h-10 overflow-hidden rounded-md">
          <img class="object-cover w-full h-full" src="/images/uploads/<%= user.profilepic %>" alt="">
        </div>
        <h3 class="text-3xl mb-3">
        <span class="font-light">Hello</span>,<%= user.name %>👋
      </div>
      </h3>
      <h5 class="underline mb-5 mt-3">You Can Create A New Post.</h5>
      <form action="/post" method="post">
        <textarea
          placeholder="What's on your mind ?"
          class="block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"
          name="content"
        ></textarea>
        <input
          class="px-3 py-2 w-40 bg-purple-500 hover:bg-purple-800 block cursor-pointer rounded-md mt-3 text-sm"
          type="submit"
          value="Create New Post"
        />
      </form>
      <div class="posts mt-20">
        <h3 class="text-zinc-400">Your Posts.</h3>
        <div class="postcontainer mt-5">
          <% user.posts.slice().reverse().forEach((post) => { %>
          <div
            class="post w-1/3 p-4 mb-4 rounded-md border-[1px] border-zinc-700 bg-zinc-800"
          >
            <h4 class="text-blue-500 mb-2"><%= user.username %></h4>
            <p class="text-sm tracking-tight"><%= post.content %></p>
            <small class="mt-2 inline-block"><%= post.likes.length %> Likes</small>
            <div class="btns flex mt-1 gap-4">
              <a class="text-blue-500 hover:text-purple-500" href="/like/<%= post._id %>">
                <%= post.likes.indexOf(user._id)===-1 ? "Like" : "Unlike" %>

              </a>
              <a class="text-zinc-600 hover:text-blue-500" href="/edit/<%= post._id %>">Edit</a>
            </div>
          </div>
          <% }) %>
        </div>
      </div>
    </div>
  </body>
</html>
